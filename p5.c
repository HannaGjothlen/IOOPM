#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(int argc, char *argv[])
{

  int tal = atoi(argv[1]);
  
  float roten = sqrt(tal);
  int limit = floor(roten) + 1;

  int x = 2;
  int y = 2;
  if (x == y)
    {
      puts("LIKA");
    }
  else
    {
      puts("OLIKA");
    }



}
  
